# 工作路线完善版：云原生平台工程、AI Infra 与 AIOps

生成日期：2026-05-08  

## 总路线

建议主线：

```text
Linux / 网络基础
    ↓
Kubernetes 深水区
    ↓
可观测性体系
    ↓
平台工程
    ↓
Service Mesh
    ↓
云原生安全与身份
    ↓
AI Infra
    ↓
AIOps / AI SRE
```

这条路线的目标不是“把更多名词写进简历”，而是把你从“系统维护者”推进到“平台建设者”，再推进到“用 AI 增强平台运维的人”。

核心方法：

- 每个阶段只抓一个主线能力，不同时学习十个方向。
- 每个阶段都要产出一个可演示、可复盘、可写进简历的真实项目。
- 学工具之前先理解数据流、控制流、故障模式和运维成本。
- 对新概念保持兴趣，但把稳定的底层能力放在前面：Linux、网络、分布式系统、Kubernetes、可观测性、身份、安全、推理系统。

## 阶段一：Linux 与网络基础

建议周期：4 到 8 周。  
目标：把线上故障最终落地的地方看懂。

你要重点理解的不是命令大全，而是这些命令背后的系统行为：

- 进程、线程、文件描述符、epoll、systemd。
- cgroup、namespace、OOM、CPU throttling、内存回收。
- TCP 建连、重传、拥塞、TIME_WAIT、keepalive、SYN backlog。
- DNS 解析链路、CoreDNS、NodeLocal DNSCache、上游 DNS。
- NAT、conntrack、iptables、ipvs、nftables。
- MTU、丢包、延迟、带宽、连接耗尽。

建议你用真实故障来学习，而不是只看教程：

- 复现一次 Nginx 502，并解释它到底是 upstream 拒绝、超时、连接池耗尽还是 DNS 问题。
- 复现一次 WebSocket 断连，并用 tcpdump、ss、日志和服务端超时参数串起来。
- 复现一次 conntrack 表满，并解释为什么服务明明活着但请求不通。
- 复现一次 Pod OOMKilled，并解释 memory request、limit、QoS、oom_score_adj 的关系。

验收标准：

- 你能画出一次请求从 client 到 Pod 再返回的路径。
- 你能区分“应用慢”“网络慢”“DNS 慢”“连接数耗尽”“CPU throttling”。
- 你能用 ss、tcpdump、dig、iptables / nft、conntrack、strace、journalctl 做初步定位。

出处：

- [Kubernetes 组件文档](https://kubernetes.io/docs/concepts/overview/components/)
- [Kubernetes 资源管理文档](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/)
- [Kubernetes Pod 生命周期文档](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/)

## 阶段二：Kubernetes 深水区

建议周期：2 到 3 个月。  
目标：从“会用 Kubernetes”变成“理解 Kubernetes 为什么这样工作”。

### 控制面

你要把下面这条链路真正吃透：

```text
kubectl apply
    ↓
apiserver
    ↓
etcd
    ↓
controller watch
    ↓
reconcile
    ↓
实际资源变化
```

重点学习：

- apiserver 如何承载 Kubernetes API。
- etcd 为什么是集群关键状态存储，为什么慢查询、磁盘延迟、网络抖动会影响控制面。
- controller 的 watch、informer、workqueue、reconcile 思想。
- scheduler 如何根据资源、亲和性、污点容忍、拓扑分布做调度。
- kubelet 如何管理 Pod 生命周期、probe、eviction、容器状态。
- CRD 与 Operator 为什么是 Kubernetes 平台扩展的关键。

实践项目：

- 写一个简单 Operator，比如自动为某类应用生成 Service、Ingress、PrometheusRule。
- 故意制造 reconcile 失败，观察事件、日志、重试和最终一致性。
- 搭一个小集群，观察 apiserver、controller-manager、scheduler、etcd 的指标。

### 数据面

重点学习：

- Pod 网络命名空间、veth pair、CNI、Service、EndpointSlice。
- kube-proxy 的 iptables / ipvs 模式。
- CoreDNS 的解析路径与缓存。
- containerd、CRI、OCI image layer、pause container。
- readiness、liveness、startup probe 的真实影响。
- HPA、VPA、Karpenter / Cluster Autoscaler 的边界。

实践项目：

- 画出 Pod 访问 Service 的网络路径。
- 复现 DNS 超时、Service 无 endpoint、Pod ready 但不可用、滚动发布中断流量等问题。
- 设计一套稳定发布基线：PDB、readiness、graceful shutdown、preStop、资源 request、HPA。

验收标准：

- 你能解释一个 Pod 从创建到 Running 的完整流程。
- 你能定位 Pod 为什么 Pending、CrashLoopBackOff、OOMKilled、NotReady。
- 你能解释 Service 为什么不通，问题是在 DNS、Endpoint、kube-proxy、CNI 还是应用。
- 你能写一个最小 Operator，并说明它的 reconcile 边界。

出处：

- [Kubernetes Components](https://kubernetes.io/docs/concepts/overview/components/)
- [Kubernetes Controllers](https://kubernetes.io/docs/concepts/architecture/controller/)
- [Kubernetes Resource Management](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/)
- [Kubernetes Pod Lifecycle](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/)
- [Kubernetes Operator Pattern](https://kubernetes.io/docs/concepts/extend-kubernetes/operator/)

## 阶段三：可观测性体系

建议周期：2 到 4 个月。  
目标：从“看 Grafana 面板”升级为“建立系统行为模型”。

### Metrics

不要只会写 `rate(xxx[5m])`。你要理解指标背后的问题类型：

- 服务层：请求量、错误率、延迟、饱和度。
- 资源层：CPU、内存、磁盘、网络、连接数、队列长度。
- 控制面：apiserver 请求延迟、etcd fsync、scheduler 调度延迟、controller workqueue。
- 业务层：订单量、消息积压、任务成功率、用户可见错误。

建议掌握：

- Google SRE 的四个黄金信号：latency、traffic、errors、saturation。
- USE 方法：utilization、saturation、errors。
- RED 方法：rate、errors、duration。
- PromQL：histogram_quantile、predict_linear、absent、label_replace、recording rules、alerting rules。

### Logging

日志不是越多越好。你要建立“能关联”的日志标准：

- 必须有 trace_id / request_id。
- 必须有 service、namespace、pod、node、version、env。
- 错误日志要带 error_code、调用目标、耗时、重试次数。
- 不要只写“error occurred”。

推荐路线：

```text
结构化日志
    ↓
日志采集与存储
    ↓
按 trace_id / request_id 关联
    ↓
和指标、链路、发布记录联动
```

Loki 更适合日志聚合和按标签查询；ClickHouse 更适合高吞吐、低成本、分析型日志平台。到底选哪一个，要由数据规模、查询模式、团队运维能力决定。

### Tracing

Tracing 的价值不是“有一张调用链图”，而是能回答：

- 慢在哪里？
- 哪个下游服务导致尾延迟？
- 哪次发布后依赖图发生变化？
- 某个错误是否集中在某个版本、区域、租户或请求类型？

建议掌握：

- trace、span、span attribute。
- context propagation。
- baggage 的适用边界。
- sampling 策略，尤其是 head sampling 与 tail sampling 的取舍。
- Metrics → Logs → Traces 的跳转链路。

实践项目：

- 给一个服务接入 Prometheus 指标、结构化日志、OpenTelemetry tracing。
- 做一张 SLO 看板，按服务展示黄金信号。
- 做一套告警规则，只保留“用户可见、需要行动、有明确处理路径”的告警。
- 为一个真实故障写 RCA，包含时间线、影响面、症状、根因、修复、长期预防。

验收标准：

- 你能解释告警为什么触发、是否值得叫醒人、怎么降噪。
- 你能从一个 5xx 或延迟告警，串到日志、链路、发布记录和 K8s 事件。
- 你能区分 symptom 和 cause，不把“CPU 高”直接当根因。

出处：

- [Google SRE：Monitoring Distributed Systems](https://sre.google/sre-book/monitoring-distributed-systems/)
- [Google SRE：Eliminating Toil](https://sre.google/sre-book/eliminating-toil/)
- [Brendan Gregg：The USE Method](https://www.brendangregg.com/usemethod.html)
- [Prometheus Query Functions](https://prometheus.io/docs/prometheus/latest/querying/functions/)
- [OpenTelemetry Traces](https://opentelemetry.io/docs/concepts/signals/traces/)
- [OpenTelemetry Context Propagation](https://opentelemetry.io/docs/concepts/context-propagation/)
- [OpenTelemetry Baggage](https://opentelemetry.io/docs/concepts/signals/baggage/)
- [OpenTelemetry Sampling](https://opentelemetry.io/docs/concepts/sampling/)

## 阶段四：平台工程

建议周期：3 到 6 个月。  
目标：把运维能力产品化，减少重复人工操作。

平台工程不是“再搭一个后台”，而是给研发提供可复用的工程能力：

- 创建服务。
- 配置 CI / CD。
- 生成 Helm / Kustomize。
- 注册服务目录。
- 接入监控、日志、告警、链路。
- 接入权限、密钥、网络策略。
- 支持灰度、回滚、发布审计。

### GitOps

要掌握的不只是 Argo CD 或 Flux 的按钮，而是 GitOps 的原则：

- 声明式配置。
- 期望状态有版本历史。
- 自动拉取期望状态。
- 持续对比实际状态并 reconcile。

实践项目：

- 用 Argo CD 管理一个多环境应用。
- 支持 dev、staging、prod 三套 overlay。
- 做自动同步、手动批准、差异检测、回滚。
- 接入 Argo Rollouts，做 canary 或 blue-green 发布。

### IaC

Terraform / OpenTofu 的重点不是会写几个 resource，而是理解：

- module 如何抽象云资源。
- state 为什么重要。
- remote backend、state locking、drift detection。
- plan / apply 如何进入审批流程。
- 如何避免把密钥和敏感输出写入状态或日志。

实践项目：

- 抽象一个“标准服务基础设施模块”：域名、证书、DNS、数据库、Redis、监控、告警。
- 做一套 plan 审批流程。
- 故意制造 drift，并用自动检测流程发现。

### 内部开发者平台

Backstage 的核心不是 UI，而是工程标准化。一个理想的服务模板应该自动生成：

- 仓库结构。
- CI 配置。
- Dockerfile。
- Helm chart 或 Kustomize overlay。
- 监控指标和告警规则。
- 日志字段规范。
- SLO 模板。
- 权限和网络策略。
- Runbook 文档。

验收标准：

- 新服务创建流程从“口头找人配置”变成“模板化、自助化、可审计”。
- 研发不需要知道每个底层平台细节，也能按标准交付服务。
- 你能解释平台边界：哪些能力平台托管，哪些责任仍属于业务团队。

出处：

- [OpenGitOps](https://opengitops.dev/)
- [Argo CD Documentation](https://argo-cd.readthedocs.io/en/stable/)
- [Flux Documentation](https://fluxcd.io/flux/)
- [Terraform State](https://developer.hashicorp.com/terraform/language/state)
- [OpenTofu State](https://opentofu.org/docs/language/state/)
- [Backstage：What is Backstage?](https://backstage.io/docs/overview/what-is-backstage/)
- [Backstage Software Templates](https://backstage.io/docs/features/software-templates/)

## 阶段五：Service Mesh

建议周期：2 到 4 个月。  
目标：理解服务间通信治理，而不是只会安装 Istio。

你已经接触 Istio Ambient，这个方向可以继续，但要避免只停留在“开了 mesh”。

重点学习：

- Envoy 的 listener、route、cluster、endpoint、filter chain。
- xDS 的 LDS、RDS、CDS、EDS，以及 ACK / NACK。
- Istio Ambient 的 ztunnel、waypoint、L4 / L7 分层。
- mTLS、workload identity、authorization policy。
- timeout、retry、circuit breaker、fault injection。
- Gateway API 与 Ingress 的差异。

实践项目：

- 做一个双版本服务，用 mesh 实现按比例灰度。
- 做故障注入，观察 timeout、retry、circuit breaker 对延迟和错误率的影响。
- 做一次 mesh 排障：为什么路由没生效、为什么 mTLS 失败、为什么 waypoint 没接管 L7。
- 对比 sidecar 模式和 ambient 模式的资源消耗、排障方式和能力边界。

验收标准：

- 你能从 Envoy 配置角度解释一次请求如何路由。
- 你能解释 Istio Ambient 里 ztunnel 和 waypoint 分别负责什么。
- 你能判断一个场景是否真的需要 Service Mesh。

出处：

- [Istio Ambient Overview](https://istio.io/latest/docs/ambient/overview/)
- [Istio Traffic Management](https://istio.io/latest/docs/concepts/traffic-management/)
- [Envoy xDS Protocol](https://www.envoyproxy.io/docs/envoy/latest/api-docs/xds_protocol)
- [Kubernetes Gateway API Overview](https://gateway-api.sigs.k8s.io/docs/concepts/api-overview/)

## 阶段六：云原生安全与身份

建议周期：贯穿平台工程与 Service Mesh 阶段。  
目标：让平台不仅能跑，还能被治理、被审计、被约束。

重点学习：

- Kubernetes admission control。
- OPA / Gatekeeper 与 Kyverno。
- Pod Security、NetworkPolicy、RBAC、ServiceAccount。
- 镜像来源、签名、准入、运行时检测。
- Falco runtime security。
- SPIFFE / SPIRE workload identity。
- OAuth 2.0、OIDC、SAML、Keycloak / Dex。

实践项目：

- 写一组准入策略：禁止 latest 镜像、必须设置 requests / limits、必须带 owner 标签、禁止特权容器。
- 用 Kyverno 或 Gatekeeper 把策略接入 GitOps。
- 用 Falco 捕获异常行为，比如容器里启动 shell、写敏感路径、异常网络连接。
- 给平台接入 OIDC 登录，并设计 RBAC 分层。
- 在 mesh 或服务间调用里引入 workload identity。

验收标准：

- 你能解释认证、授权、准入控制、运行时检测的区别。
- 你能解释 OIDC 和 OAuth2 的关系。
- 你能写出一套“上线前阻断、运行中检测、事后审计”的安全闭环。

出处：

- [OPA for Kubernetes Admission Control](https://www.openpolicyagent.org/docs/kubernetes)
- [Kyverno：How Kyverno Works](https://kyverno.io/docs/introduction/how-kyverno-works/)
- [Falco Documentation](https://falco.org/docs/)
- [SPIRE Concepts](https://spiffe.io/docs/latest/spire-about/spire-concepts/)
- [OpenID Connect Core 1.0](https://openid.net/specs/openid-connect-core-1_0.html)
- [OAuth 2.0 RFC 6749](https://www.rfc-editor.org/rfc/rfc6749)
- [SAML v2.0 OASIS Standard](https://www.oasis-open.org/standard/saml/)
- [Keycloak Documentation](https://www.keycloak.org/documentation)

## 阶段七：AI Infra

建议周期：4 到 8 个月。  
目标：从“会调 LLM API”升级到“理解 LLM 推理、服务化、成本和稳定性”。

### LLM 推理

重点学习：

- token、context window、prefill、decode。
- KV cache 为什么决定吞吐、并发和显存。
- batching、continuous batching、paged attention。
- 量化、speculative decoding、tensor parallel。
- GPU 显存、MIG、NCCL、多卡通信。
- 推理服务指标：TTFT、TPOT、tokens/s、并发、排队延迟、显存水位、KV cache 命中。

实践项目：

- 用 vLLM 或 SGLang 部署一个 OpenAI-compatible API。
- 接入 Prometheus / Grafana，观测吞吐、延迟、队列和显存。
- 对比不同模型、并发、batch、量化方式的延迟与成本。
- 做一次“容量评估”：给定 QPS、平均输出 token、P95 延迟目标，需要多少 GPU。

### RAG

重点不是“套 LangChain”，而是把 RAG 当成检索系统来做：

```text
文档解析
    ↓
切分
    ↓
embedding
    ↓
索引
    ↓
retrieval
    ↓
rerank
    ↓
prompt assembly
    ↓
generation
    ↓
评估
```

你要关注：

- 召回率、精排质量、上下文污染。
- chunk 粒度、metadata、权限过滤。
- 向量库不是越复杂越好，小规模可以先用 pgvector。
- RAG 的核心风险是“看似引用了资料，但实际答非所问”。

### Agent

Agent 不要从框架名开始学，要从生产约束开始：

- 工具权限。
- 人工确认。
- 失败重试。
- 状态持久化。
- 审计日志。
- 成本上限。
- 输入输出 guardrails。
- 对危险操作分级：只读、低风险写入、高风险写入。

实践项目：

- 做一个只读 AI Runbook Agent：输入告警，自动查 Prometheus、K8s events、日志、trace，输出 RCA 草稿。
- 第二阶段再加入“建议操作”，但不自动执行。
- 第三阶段才考虑带审批的自动修复，例如重启无状态 Pod、扩容副本、回滚到上一版本。

验收标准：

- 你能解释 vLLM / SGLang / TensorRT-LLM 分别解决什么问题。
- 你能给一个 LLM 服务做容量评估和成本估算。
- 你能设计一个有权限边界、审计、回滚和人工确认的 Agent。

出处：

- [vLLM Documentation](https://docs.vllm.ai/en/stable/)
- [PagedAttention 论文](https://arxiv.org/abs/2309.06180)
- [SGLang Documentation](https://docs.sglang.io/)
- [SGLang 论文](https://arxiv.org/abs/2312.07104)
- [NVIDIA TensorRT-LLM](https://docs.nvidia.com/tensorrt-llm/)
- [NVIDIA CUDA C++ Programming Guide](https://docs.nvidia.com/cuda/cuda-c-programming-guide/index.html)
- [NVIDIA NCCL Documentation](https://docs.nvidia.com/deeplearning/nccl/)
- [NVIDIA MIG User Guide](https://docs.nvidia.com/datacenter/tesla/mig-user-guide/)
- [LangGraph Workflows and Agents](https://docs.langchain.com/oss/python/langgraph/workflows-agents)
- [OpenAI Agents SDK](https://developers.openai.com/api/docs/guides/agents)
- [OpenAI Agents SDK Guardrails](https://openai.github.io/openai-agents-python/guardrails/)
- [Model Context Protocol Specification](https://modelcontextprotocol.io/specification/2025-06-18)

## 阶段八：AIOps / AI SRE

建议周期：长期主线。  
目标：把你已有的 K8s、监控、告警、日志、AI Agent 能力融合成差异化优势。

第一阶段先做只读分析：

```text
告警
    ↓
查指标
    ↓
查日志
    ↓
查 trace
    ↓
查 K8s Event
    ↓
查最近变更
    ↓
关联分析
    ↓
输出 RCA 草稿
```

第二阶段做 Runbook 自动化：

- 自动收集上下文。
- 自动匹配已有 Runbook。
- 自动生成排查步骤。
- 自动生成风险说明。
- 自动提醒需要人工确认的操作。

第三阶段再做受控自动修复：

- 只允许低风险动作先自动化。
- 每个动作必须有前置条件、影响范围、回滚方案。
- 所有动作必须有审计日志。
- 高风险动作必须人工确认。
- 自动修复失败要能停止，而不是继续扩大影响。

你可以做的代表性项目：

- 告警降噪系统：把重复告警按服务、版本、节点、变更窗口聚合。
- RCA 助手：输入 PagerDuty / 飞书告警，输出时间线、可能根因、证据和下一步。
- 发布影响分析：把 GitOps 变更、K8s event、指标异常、日志错误关联起来。
- AI Runbook：把 PromQL、kubectl、日志查询、trace 查询封装成只读工具。
- 带审批的修复系统：扩容、回滚、重启、限流都需要策略约束。

验收标准：

- 输出不是“猜测根因”，而是“证据链 + 置信度 + 下一步验证”。
- 系统能明确说“不足以判断”，并列出缺失证据。
- 自动化先降低排查时间，再谈自动修复。

出处：

- [Google SRE：Monitoring Distributed Systems](https://sre.google/sre-book/monitoring-distributed-systems/)
- [Google SRE：Eliminating Toil](https://sre.google/sre-book/eliminating-toil/)
- [OpenTelemetry Traces](https://opentelemetry.io/docs/concepts/signals/traces/)
- [OpenAI Agents SDK Guardrails](https://openai.github.io/openai-agents-python/guardrails/)

## 12 个月执行建议

第 1 到 2 个月：

- 补 Linux / 网络 / K8s 数据面。
- 做 3 个故障复现实验：DNS、conntrack、OOM。
- 输出一份《K8s 请求路径与故障定位笔记》。

第 3 到 4 个月：

- 深入 K8s 控制面和 Operator。
- 写一个最小 Operator。
- 做一份《Pod 创建到 Running 的控制面链路图》。

第 5 到 6 个月：

- 做可观测性体系。
- 建 SLO 面板、告警规则、日志规范、trace 链路。
- 输出一个真实服务的 RCA 模板。

第 7 到 8 个月：

- 做 GitOps 和平台化。
- 用 Argo CD 管理多环境应用。
- 用 Backstage 或轻量模板系统生成标准服务脚手架。

第 9 到 10 个月：

- 深入 Service Mesh、安全和身份。
- 做 Ambient / waypoint / mTLS / traffic policy 实验。
- 写准入策略和运行时检测。

第 11 到 12 个月：

- 做 AI Infra 和 AI Runbook。
- 部署 vLLM / SGLang。
- 把 Prometheus、日志、K8s event、trace 查询封装为 Agent 工具。
- 输出一个只读 RCA Agent。

## 最适合沉淀的作品集

作品一：Kubernetes 故障实验室

- 包含 DNS、OOM、CPU throttling、conntrack、Service endpoint、rollout、probe 等故障。
- 每个故障都有复现脚本、观测指标、排查路径、修复方式。
- 简历价值：证明你不是只会部署，而是真能定位问题。

作品二：可观测性标准方案

- Prometheus + Grafana + OpenTelemetry + 日志系统。
- 每个服务自动生成 dashboard、alert、runbook。
- 支持从告警跳到日志和 trace。
- 简历价值：证明你能建设体系，而不是只会看面板。

作品三：内部开发者平台原型

- 一键创建服务。
- 自动生成 CI、Helm、监控、告警、文档。
- GitOps 发布。
- 简历价值：证明你具备平台工程思维。

作品四：AI Runbook / RCA Agent

- 告警输入。
- 自动查询 PromQL、K8s event、日志、trace、最近发布。
- 输出证据链、可能根因、下一步操作。
- 高风险动作只给建议，不直接执行。
- 简历价值：这是你把 SRE 和 AI Infra 结合起来的关键作品。

## 避免的坑

不要只学框架 API：

- 框架会换，系统原理不会那么快换。
- 只会调 API，遇到性能、权限、网络、数据一致性问题会断层。

不要同时学十个方向：

- 你的路线已经足够宽。
- 真正拉开差距的是深度和能交付的系统。

不要沉迷新概念：

- MCP、AutoGen、Browser Use、各类 Agent 框架都值得关注，但不应该压过底层能力。
- 先学会把工具放进有边界、有审计、有回滚的系统。

不要把“自动修复”做太早：

- 自动诊断先于自动修复。
- 只读 Agent 先于可写 Agent。
- 低风险动作先于高风险动作。
- 没有审计、回滚、人工确认，就不要自动操作生产。

不要把简历写成技术名词堆砌：

- 弱写法：熟悉 K8s、Prometheus、Istio、AI Agent。
- 强写法：设计并落地 K8s 故障诊断体系，将告警、日志、trace、K8s event 与发布记录关联，缩短故障定位时间。
- 弱写法：使用 Argo CD。
- 强写法：基于 GitOps 管理多环境发布，接入自动差异检测、灰度发布、回滚与发布审计。

## 最终职业定位

更适合你的定位不是传统运维，而是下面这些方向的交集：

- Cloud Native Platform Engineer。
- SRE / Staff SRE。
- AI Infra Engineer。
- AIOps Engineer / Architect。
- 云原生平台架构师。

一句话定位：

> 你要把“会维护系统”升级成“会建设平台”，再把“会建设平台”升级成“能用 AI 提升平台稳定性与运维效率”。

这条路线最重要的不是学完多少工具，而是持续产出能证明你能力的系统：故障实验室、可观测性标准、平台模板、AI Runbook、RCA Agent。
