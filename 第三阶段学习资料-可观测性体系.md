# 第三阶段学习资料：可观测性体系

生成日期：2026-05-08  
对应路线文档：[工作路线完善版.md](工作路线完善版.md)

## 目标

第三阶段的目标不是“搭一套 Prometheus + Grafana”，而是建立系统行为模型。

阶段结束时，你应该能做到：

- 为一个服务设计 RED 指标、USE 指标、SLO 和告警规则。
- 从告警跳到 dashboard、日志、trace、K8s Event 和发布记录。
- 区分 symptom 和 root cause，不把“CPU 高”“Pod 重启”“延迟高”直接当根因。
- 能写 PromQL，包括 `rate`、`histogram_quantile`、`absent`、`predict_linear`、recording rules 和 alerting rules。
- 能设计结构化日志字段，并避免 Loki 高基数 label 坑。
- 能理解 trace、span、context propagation、baggage、sampling。
- 能写一份清晰 RCA，包含时间线、影响、证据、根因、修复和预防。

## 建议环境

建议沿用第二阶段 kind 集群，另准备：

- Helm。
- Prometheus / Grafana，建议用 `kube-prometheus-stack`。
- OpenTelemetry Collector。
- 一个可观测性测试服务，例如 OpenTelemetry Demo、自己写的 HTTP 服务、或 `nginx` + `curl` + 简单压测。
- Loki 或 ClickHouse 二选一，初学建议先 Loki。
- Jaeger 或 Tempo 二选一，初学建议先 Jaeger，后续再看 Tempo。

本地实验可以这样开头：

```bash
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo add grafana https://grafana.github.io/helm-charts
helm repo update

kubectl create namespace observability

helm install kps prometheus-community/kube-prometheus-stack \
  -n observability
```

## 学习顺序

推荐顺序：

```text
监控目标与 SLO
    ↓
Metrics 与 PromQL
    ↓
Grafana dashboard 设计
    ↓
Alerting 与降噪
    ↓
Logging 与结构化日志
    ↓
Tracing 与 OpenTelemetry
    ↓
Metrics / Logs / Traces 联动
    ↓
RCA 与可观测性标准化
```

## 第 1 周：SLO、黄金信号与监控模型

### 要理解什么

监控不是“什么都采”。监控首先要回答：

- 用户是否受影响？
- 哪个服务受影响？
- 影响程度有多大？
- 是否需要叫醒人？
- 下一步排查入口在哪里？

重点概念：

- Google SRE 四个黄金信号：latency、traffic、errors、saturation。
- RED：Rate、Errors、Duration。
- USE：Utilization、Saturation、Errors。
- SLI、SLO、Error Budget。
- Page alert 与 ticket alert 的区别。
- 症状告警优先于原因告警。

### 必读资料

- [Google SRE：Monitoring Distributed Systems](https://sre.google/sre-book/monitoring-distributed-systems/)
- [Google SRE：Service Level Objectives](https://sre.google/sre-book/service-level-objectives/)
- [Brendan Gregg：The USE Method](https://www.brendangregg.com/usemethod.html)

### 实验一：为一个服务写 SLO 草案

选择一个 HTTP 服务，写下：

```text
服务名称：
用户入口：
核心接口：
用户可见失败定义：
可接受延迟：
可接受错误率：
SLO：
排除项：
告警条件：
Runbook 链接：
```

你要回答：

- 这个服务最重要的用户路径是什么？
- 哪些指标能代表用户体验？
- 哪些告警应该 page，哪些只进 ticket？
- 如果只有 CPU、内存、Pod 重启，为什么还不够？

## 第 2 周：Prometheus 与 PromQL

### 要理解什么

Prometheus 的核心是时序数据和标签。你要理解：

- metric name 与 labels。
- counter、gauge、histogram、summary。
- scrape interval。
- PromQL instant vector、range vector。
- cardinality 为什么会毁掉 Prometheus。
- recording rules 用于预计算常用表达式。
- alerting rules 用于定义告警条件。

### 必读资料

- [Prometheus：Querying basics](https://prometheus.io/docs/prometheus/latest/querying/basics/)
- [Prometheus：Query functions](https://prometheus.io/docs/prometheus/latest/querying/functions/)
- [Prometheus：Alerting rules](https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/)
- [Prometheus：Recording rules](https://prometheus.io/docs/prometheus/latest/configuration/recording_rules/)

### 实验二：写核心 PromQL

先转发 Prometheus：

```bash
kubectl -n observability port-forward svc/kps-kube-prometheus-stack-prometheus 9090:9090
```

练习这些查询：

```promql
up
rate(container_cpu_usage_seconds_total[5m])
sum by (namespace, pod) (rate(container_cpu_usage_seconds_total[5m]))
container_memory_working_set_bytes
rate(http_requests_total[5m])
sum by (code) (rate(http_requests_total[5m]))
histogram_quantile(0.95, sum by (le) (rate(http_request_duration_seconds_bucket[5m])))
absent(up{job="some-job"})
predict_linear(node_filesystem_avail_bytes[6h], 24 * 3600)
```

你要回答：

- `rate` 为什么不能直接用于 gauge？
- `histogram_quantile` 为什么需要 `_bucket`？
- label 太多为什么会导致高基数问题？
- `absent` 适合什么告警？
- `predict_linear` 适合什么场景，不适合什么场景？

## 第 3 周：Kubernetes 与基础设施指标

### 要理解什么

Kubernetes 监控要覆盖几层：

- Node：CPU、memory、disk、network、filesystem、load。
- Pod / Container：CPU、memory、restart、OOM、throttling。
- Control Plane：apiserver、etcd、scheduler、controller-manager。
- Workload：Deployment 可用副本、rollout 状态、HPA 状态。
- Data Plane：Service endpoint、CoreDNS、Ingress / Gateway。

### 必读资料

- [Kubernetes Components](https://kubernetes.io/docs/concepts/overview/components/)
- [Kubernetes System Component Metrics](https://kubernetes.io/docs/concepts/cluster-administration/system-metrics/)
- [Kubernetes System Traces](https://kubernetes.io/docs/concepts/cluster-administration/system-traces/)

### 实验三：做 K8s 基础排障面板

Grafana 面板至少包含：

- namespace / workload 选择器。
- Pod restart 数。
- CPU usage 与 CPU request 对比。
- memory working set 与 memory limit 对比。
- CPU throttling。
- Pod phase。
- Deployment available replicas。
- apiserver request latency。
- CoreDNS 请求量与错误。

你要回答：

- CPU usage 高和 CPU throttling 是一回事吗？
- memory working set 接近 limit 后有什么风险？
- Pod restart 与 CrashLoopBackOff 如何关联？
- 控制面延迟会如何影响用户操作？

## 第 4 周：告警规则与降噪

### 要理解什么

告警规则不是越多越好。有效告警必须满足：

- 用户可能受影响。
- 需要人工或自动化行动。
- 有明确 owner。
- 有 runbook。
- 有合理 `for`，避免瞬时抖动。
- 有 severity 区分。

告警分层：

- Page：马上处理，例如核心服务不可用、错误率过高、SLO 快速燃烧。
- Ticket：需要处理但不紧急，例如磁盘一周后可能满。
- Info：只用于诊断，不触发人。

### 必读资料

- [Prometheus：Alerting rules](https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/)
- [Google SRE：Practical Alerting](https://sre.google/sre-book/practical-alerting/)

### 实验四：写告警规则

创建一个规则文件：

```yaml
groups:
- name: service-slo
  rules:
  - alert: HighErrorRate
    expr: |
      sum(rate(http_requests_total{code=~"5.."}[5m]))
      /
      sum(rate(http_requests_total[5m])) > 0.05
    for: 10m
    labels:
      severity: page
    annotations:
      summary: "High 5xx error rate"
      runbook_url: "https://example.com/runbooks/high-error-rate"
```

你要回答：

- 为什么要有 `for`？
- 为什么告警表达式要尽量从用户症状出发？
- 告警 annotation 里应该放哪些排查入口？
- 一个告警没有 runbook，为什么不是好告警？

## 第 5 周：Logging 与结构化日志

### 要理解什么

日志系统的关键不是“存下所有文本”，而是让日志能关联、能过滤、能解释。

结构化日志建议字段：

- `timestamp`
- `level`
- `service.name`
- `deployment.environment`
- `k8s.namespace.name`
- `k8s.pod.name`
- `trace_id`
- `span_id`
- `request_id`
- `user_id` 或 tenant_id，注意脱敏和权限
- `route`
- `status_code`
- `duration_ms`
- `error_code`

Loki 的关键点：

- labels 用于低基数字段。
- 高基数字段不要做 index label。
- trace_id、pod name、user_id 更适合 structured metadata 或日志字段。

ClickHouse 的关键点：

- 适合高吞吐分析型查询。
- 设计表结构和分区时要考虑查询模式。
- 适合做日志分析平台，但运维成本高于入门级 Loki。

### 必读资料

- [Grafana Loki：Understand labels](https://grafana.com/docs/loki/latest/get-started/labels/)
- [Grafana Loki：Structured metadata](https://grafana.com/docs/loki/latest/get-started/labels/structured-metadata/)
- [OpenTelemetry Logging](https://opentelemetry.io/docs/specs/otel/logs/)
- [ClickHouse Observability](https://clickhouse.com/use-cases/observability)

### 实验五：日志字段标准

为你的服务写一份日志规范：

```text
必须字段：
推荐字段：
禁止字段：
脱敏规则：
trace_id 注入方式：
错误日志格式：
采集路径：
保留周期：
查询示例：
```

你要回答：

- 为什么不能把 user_id 做 Loki label？
- 为什么 trace_id 必须能和 trace 系统关联？
- 日志里能不能打印 token、cookie、手机号？
- 错误日志应该记录错误码还是完整堆栈，边界是什么？

## 第 6 周：Tracing 与 OpenTelemetry

### 要理解什么

Tracing 用来回答请求跨服务流转的问题：

- 请求经过哪些服务？
- 慢在哪个 span？
- 哪个下游失败？
- 上下游 trace_id 是否连续？
- 采样策略是否保留了错误和慢请求？

重点概念：

- Trace。
- Span。
- Span kind。
- Span attributes。
- Context propagation。
- Baggage。
- Head sampling。
- Tail sampling。
- OpenTelemetry Collector。

### 必读资料

- [OpenTelemetry Traces](https://opentelemetry.io/docs/concepts/signals/traces/)
- [OpenTelemetry Context Propagation](https://opentelemetry.io/docs/concepts/context-propagation/)
- [OpenTelemetry Baggage](https://opentelemetry.io/docs/concepts/signals/baggage/)
- [OpenTelemetry Sampling](https://opentelemetry.io/docs/concepts/sampling/)
- [OpenTelemetry Semantic Conventions](https://opentelemetry.io/docs/concepts/semantic-conventions/)

### 实验六：接入最小 trace

目标：

```text
service-a
    ↓ HTTP
service-b
```

要求：

- service-a 接收请求生成 trace。
- service-a 调 service-b 时传播 trace context。
- service-b 的 span 能挂到同一条 trace 下。
- 日志里带 trace_id。
- Grafana / Jaeger / Tempo 能按 trace_id 查到链路。

你要回答：

- trace 断裂通常由什么导致？
- baggage 和 span attribute 有什么区别？
- 为什么 100% tracing 在高流量系统里可能不可行？
- tail sampling 为什么适合保留错误请求和慢请求？

## 第 7 周：Metrics、Logs、Traces 联动

### 要理解什么

现代可观测性要实现跳转链路：

```text
告警
    ↓
Dashboard
    ↓
异常指标
    ↓
相关日志
    ↓
相关 trace
    ↓
K8s Event / 发布记录
    ↓
RCA
```

你要设计统一标签：

- `service.name`
- `service.namespace`
- `deployment.environment`
- `k8s.cluster.name`
- `k8s.namespace.name`
- `k8s.pod.name`
- `service.version`
- `trace_id`

### 实验七：一次完整排障演练

制造一次 5xx：

- 发布错误版本。
- 或让下游服务返回 500。
- 或通过 Istio / Nginx 注入故障。

排查路径必须包含：

- 触发告警。
- 查看错误率面板。
- 找到对应服务版本。
- 查询日志。
- 查询 trace。
- 查看 K8s Event。
- 查看最近发布。
- 写 RCA。

你要回答：

- 哪个证据证明这是用户可见问题？
- 哪个证据证明根因不是 CPU / 内存？
- 哪个证据证明与发布有关或无关？
- 哪个动作能最快止血？

## 第 8 周：RCA、标准化与作品集

### RCA 模板

```text
标题：
时间：
影响范围：
用户影响：
发现方式：
时间线：
直接原因：
根因：
证据：
止血动作：
长期修复：
预防措施：
告警改进：
Runbook 改进：
负责人：
截止日期：
```

### 作品集项目

做一个“服务可观测性标准包”：

- Dashboard JSON。
- PrometheusRule。
- 日志字段规范。
- Trace 接入说明。
- Runbook。
- RCA 模板。
- 服务接入 checklist。

## 命令清单

Prometheus / Grafana：

```bash
kubectl -n observability get pods
kubectl -n observability port-forward svc/kps-kube-prometheus-stack-prometheus 9090:9090
kubectl -n observability port-forward svc/kps-grafana 3000:80
kubectl get servicemonitor -A
kubectl get prometheusrule -A
```

Kubernetes 事件：

```bash
kubectl get events --sort-by=.metadata.creationTimestamp
kubectl describe pod <pod>
kubectl logs <pod>
kubectl logs <pod> --previous
```

PromQL 检查：

```bash
promtool check rules rules.yaml
promtool query instant http://localhost:9090 'up'
```

## 每周产出

第 1 周：

- 一份服务 SLO 草案。

第 2 周：

- 一组 PromQL 查询笔记。

第 3 周：

- 一张 K8s 基础排障 dashboard。

第 4 周：

- 一组 PrometheusRule 和告警分级说明。

第 5 周：

- 一份结构化日志规范。

第 6 周：

- 一条跨服务 trace 示例。

第 7 周：

- 一份完整排障演练记录。

第 8 周：

- 一套服务可观测性标准包。

## 自测题

- RED 和 USE 分别适合看什么？
- SLI 和 SLO 的区别是什么？
- 什么告警应该 page？
- `rate`、`increase`、`irate` 的区别是什么？
- histogram 如何计算 P95？
- 高基数 label 为什么危险？
- Loki label 和 structured metadata 的边界是什么？
- trace 断裂一般怎么排查？
- head sampling 和 tail sampling 的差异是什么？
- 为什么 RCA 里要区分直接原因和根因？

## 完成标准

你可以认为第三阶段过关的标准是：

- 能为一个服务完整设计 metrics、logs、traces、dashboard、alert、runbook。
- 能从一个告警一路追到日志、trace、K8s Event 和发布记录。
- 能写一份不甩锅、证据充分、可执行改进的 RCA。
- 能解释可观测性成本，包括指标基数、日志量、trace 采样、存储周期。
