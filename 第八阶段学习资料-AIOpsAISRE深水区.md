# 第八阶段学习资料（进阶）：AIOps / AI SRE 深水区

生成日期：2026-08-24
对应路线文档：[工作路线完善版.md](工作路线完善版.md)
前置：完成[第八阶段基础篇](第八阶段学习资料-AIOps与AISRE.md)；理论配套 ai-ops-learning 仓库的 `ai-agent-ecosystem.md`

## 为什么需要这一篇

基础篇教了"怎么搭一个只读 RCA Agent"。但 2026 年 AI SRE 已经从"能 demo"变成"敢不敢上生产"的问题，而这道坎上倒下的项目，几乎都不是倒在"Agent 不会查"，而是倒在三件事：

1. **没有评估体系**——演示很炫，但没人能回答"它到底多准""换个 prompt 是变好还是变坏"，于是永远不敢给它真正的权限。
2. **告警关联做得幼稚**——把告警风暴一条条丢给 LLM，token 烧光、还抓不住"这 200 条其实是一个故障"。
3. **修复系统的安全边界是 prompt 写的**——"你不许执行危险操作"挡不住任何真实事故，第一次误操作就前功尽弃。

先给两个工业锚点，校准你的预期：

- Meta 的生产 RCA 系统（微调 Llama + 启发式检索 + 排序），在创建工单的第一时间给出根因的**准确率是 42%**——注意这是"Top-1 命中一个候选集"的 42%，且被他们认为很有价值。
- 微软 RCACopilot（已在内部用了 4 年）报告的 RCA 准确率**约 0.77**，靠的是"诊断信息收集"组件多年沉淀，不是模型本身。

**结论：不要追求"AI 自动定位根因"的高准确率幻觉。真正落地的系统，价值在"把证据收集和关联自动化，把人从翻数据里解放出来"，根因判断仍是人机协作。** 这一篇就是教你怎么把这件事做扎实。

阶段结束时，你应该能做到：

- 为一个 RCA Agent 建立离线评估集和评估流水线，用数字说话而不是靠 demo 感觉。
- 设计告警关联/降噪的多级策略（规则 → 拓扑 → 语义），并知道各自的边界。
- 设计一个带审批、能回滚、失败能停的自动修复系统，安全边界建立在硬机制而非 prompt 上。
- 说清 AI SRE 的责任模型：出了事谁负责、如何审计、如何灰度。

## 学习顺序

```
AI SRE 能力分级与责任模型
    ↓
评估工程（离线评估集 → 在线影子模式 → 回归）
    ↓
告警关联与降噪（规则 / 拓扑 / 语义 三级）
    ↓
上下文工程（决定成败的隐藏变量）
    ↓
带审批的自动修复（安全边界的硬机制）
    ↓
生产化与治理（灰度、审计、成本、责任）
```

---

## 第 1 周：能力分级与责任模型

### 要理解什么

AI SRE 不是一个二元的"有/没有"，是一条分级的路。照搬自动驾驶 L0-L5 的分级，能让你和管理层、和自己都讲清楚"现在在哪、下一步去哪":

```
L0 人工：人查一切
L1 辅助：AI 做摘要/建议，人查人做           ← 多数团队 2026 年的现实起点
L2 部分自动：AI 自动收集上下文+给候选根因，人确认    ← 本阶段主攻
L3 条件自动：低风险场景 AI 端到端处置，人事后审计
L4 高度自动：多数场景自主，人只管例外
L5 完全自动：无人值守
```

关键认知：**每往上一级，门槛不是"模型更强"，而是"评估更可信 + 边界更硬 + 审计更全"**。跳级必翻车。

### 责任模型（上生产前必须先答的问题）

```
AI 给出错误根因，导致人误操作 → 谁负责？
AI 自动执行了修复，扩大了故障 → 谁负责？
AI 该报警没报 → 谁负责？

务实答案：AI 是"工具"不是"责任主体"。
  - L0-L2：人对最终决策负全责，AI 输出只是证据，必须可追溯到原始数据
  - L3+：定义清楚"自动化的责任归属"（通常是 owning team + 平台团队共担），
         且每个自动动作必须有"当时的决策依据快照"存档
```

### 必读资料

- [Google SRE Workbook：Incident Response](https://sre.google/workbook/incident-response/)
- [Meta：Leveraging AI for efficient incident response](https://engineering.fb.com/2024/06/24/data-infrastructure/leveraging-ai-for-efficient-incident-response/)
- [Microsoft：Automatic Root Cause Analysis via LLMs for Cloud Incidents（RCACopilot）](https://www.microsoft.com/en-us/research/publication/automatic-root-cause-analysis-via-large-language-models-for-cloud-incidents/)

### 实验一：给你的团队定级

写一页纸：

```
当前处在哪一级？证据是什么（哪些事 AI 已在做）？
下一级的三个卡点分别是什么？
如果明天要往上一级，你最担心的失败场景是什么？对应的兜底是什么？
```

---

## 第 2 周：评估工程（这一周决定项目生死）

### 要理解什么

没有评估的 AI SRE = 没有监控的生产系统。你不会把没监控的服务上线，也别把没评估的 Agent 上线。评估分三层，缺一不可：

```
① 离线评估集（offline eval）：固定的历史事故集，每次改动都回归
② 影子模式（shadow mode）：真实告警并行喂给 Agent，只看不动，与人的结论对比
③ 在线指标（online）：真正接管后，持续跟踪 MTTR、误报、人工推翻率
```

### 离线评估集怎么建

从历史事故里挑 30-50 个，覆盖不同类型，每个样本冻结"事故发生那一刻能拿到的数据快照"：

```
样本结构：
  incident_id
  输入：告警内容 + 那一刻可查的指标/日志/trace/近期变更（快照，不是现在的数据！）
  标准答案：
    root_cause（人工复盘确认的）
    root_cause_category（发布/资源/依赖/网络/配置/外部...）
    key_evidence（哪几条证据是定位关键）
  边界标注：
    这个案例证据是否充分？（有一部分应该标"证据不足，正确反应是升级给人"）
    允许的处置动作 / 禁止的处置动作
```

**最容易被忽略、却最重要的一类样本：证据不足的案例**。一个成熟的 RCA Agent 的核心能力之一是"知道自己不知道"——能说"证据不足以判断，建议人工介入并补充 X 数据"，而不是每次都硬编一个根因。评估集里必须有 20-30% 这类样本专门考这个。

### 评估指标（分开评"检索"和"判断"）

RAG 那套评估思想直接搬过来，但要拆成两段——因为 Meta/微软的经验都指向同一个事实：**RCA 系统的准确率上限主要由"上下文收集/检索"决定，不是由 LLM 的推理决定**。

```
检索/收集层（这层烂，后面全烂）：
  上下文召回率：定位根因需要的关键证据，收集到了几个？
  变更定位命中率：真凶变更是否进了候选集？（Meta 的做法是把候选压到 ≤20 个变更再让 LLM 选 top5）

判断层：
  根因 Top-1 准确率 / Top-3 准确率（对标 Meta 42%、微软 0.77，别给自己定 90%）
  类别准确率（判对"是发布问题"比判对"是哪个 commit"容易，也常常够用）
  弃权正确率：证据不足样本里，正确说"判不了"的比例  ← 安全性的核心指标
  幻觉率：编造了数据里不存在的"证据"的比例  ← 必须趋近 0
```

### 影子模式

离线过关后、接管前的必经阶段：真实告警同时喂给 Agent 和人，Agent 的结论**只记录不展示**（避免锚定人的判断），事后比对。

```
影子期至少 2-4 周，重点看：
  Agent 的结论和人的最终复盘一致的比例
  Agent 平均比人快多久给出候选（这是它的核心价值证明）
  分类型统计：哪类事故它靠谱、哪类它坑（据此定"它先接管哪类"）
```

### 必读资料

- [Meta AI-Assisted RCA（ZenML 案例拆解）](https://www.zenml.io/llmops-database/ai-assisted-root-cause-analysis-system-for-incident-response)
- RAG 评估迁移思路见 ai-ops-learning 仓库 `rag-implementation-guide.md` 的 RAGAS/DeepEval 章节

**一手生产数据（作者博客）**

- [我的告警系统把 90% 的告警判成 high，独立调查器只认同 26%](https://blog.wetalk.eu.org/ops/services/severity-calibration/)——用独立评判器校准 LLM 分级，并公开分歧率。这就是本周"分开评检索和判断"在生产里的样子：评判器和被评系统必须独立，否则一致率没有意义。
- [让两个告警系统在真实流量上对赌：影子灰度跑出的第一批数据](https://blog.wetalk.eu.org/ops/services/shadow-run-two-alert-systems/)——影子模式的完整实施记录，含第一批对比数据怎么读。
- [决策档案里最值钱的是 rejected 桶](https://blog.wetalk.eu.org/ops/rejected-notes-bucket/)——评估集里负样本（被否决的决策）为什么比正样本更值钱。

**延伸阅读（公众号文章，须带批判性读）**

- 《当 AI 做根因分析时，它在"胡说八道"什么？——从 Datadog Bits AI 的假设-验证方法论谈起》（公众号 SRETALK）——把 RCA 建模为"假设生成 → 逐条验证"而非一次性结论，是评估工程该采用的框架。Datadog 能力清单逐项核对属实，仅视图名称与官方不一致（官方为 Investigation Steps / Hypothesis Tree）。
- 《AIOps 实战：小红书和腾讯教你如何做根因分析》（公众号 CIT云原生）——两家的 RCA 方案对比有参考价值。注意：文中"定位准确率 80%+"实为 Top-5 命中率，不是点中率；RCSF 是学术文献方法而非小红书自研。

### 实验二：建 20 个样本的评估集 + 一版评估脚本

```
1. 从历史事故挑 20 个（含 5 个"证据不足"样本），冻结数据快照
2. 写评估脚本：喂给 Agent → 自动比对 → 输出上面那套指标
3. 关键动作：故意改坏 prompt（比如删掉"证据不足时要弃权"那句），
   看哪个指标掉了——这验证了你的评估集真的能发现回归
```

产出：一个能一键跑、输出指标表的评估流水线。这是本阶段最有价值的交付物，比 Agent 本身更重要。

---

## 第 3 周：告警关联与降噪

### 要理解什么

生产事故的典型形态不是"一条告警"，是"3 分钟内 200 条告警"。直接把 200 条丢给 LLM 是新手最大的错误：token 爆炸、成本失控、而且效果差（大海捞针）。正确姿势是**先用便宜手段把 200 条压成"1 个事件 + 结构化摘要"，再动用 LLM**。

三级关联，从便宜到贵：

```
① 规则/分组关联（最便宜，先做）
   按 alertname + 时间窗 + 公共 label（service/cluster/namespace）聚合
   Alertmanager 的 group_by 本身就是第一道，别浪费
   去重：同一 alert 的 firing/resolved 抖动折叠

② 拓扑/因果关联（中等，最有价值）
   利用服务依赖图：A 依赖 B，B 挂了 → A 的告警是"症状"，B 的是"根因侧"
   利用时间先后：先烧起来的服务更可能是源头
   利用变更事件：关联窗口内的 deploy/config change（Meta 的核心信号）
   产出："这 200 条 = 1 个事件，疑似源头是 B 服务，触发点是 14:30 的 B 发布"

③ 语义关联（最贵，收尾用）
   到这一步才上 LLM：对已经聚合好的少量候选做根因推理和自然语言总结
   输入是结构化的关联结果，不是原始 200 条
```

### 降噪的量化目标

```
输入：原始告警条数
输出：需要人看的"事件"数
压缩比 = 输入/输出，健康的系统应到 10:1 甚至 100:1
但要盯住反指标：漏报率（把真问题降噪掉了）—— 这个必须趋近 0，
                降噪宁可多留几条，不能吃掉真事故
```

### 一个务实的架构

```
Alertmanager (group_by 一级聚合)
    ↓ webhook
关联引擎（规则 + 拓扑，无 LLM）
    ├── 查服务依赖图（来自 mesh/APM/CMDB）
    ├── 查关联窗口内的变更（GitOps commit / Argo 同步记录）
    └── 输出：Incident 对象（含聚合的告警、疑似源头、相关变更）
    ↓ 仅对"新 Incident"触发
LLM 层（RCA Agent）
    → 基于结构化 Incident 做根因候选 + 证据链 + 自然语言摘要
    → 推送给值班人（L2）或进入修复决策（L3+）
```

**关键设计：LLM 只在"事件级"触发，不在"告警级"触发**。这一个设计能把你的 LLM 成本降一到两个数量级。

### 必读资料

- [Prometheus Alertmanager：Grouping / Inhibition](https://prometheus.io/docs/alerting/latest/alertmanager/)
- [OpenTelemetry Semantic Conventions](https://opentelemetry.io/docs/concepts/semantic-conventions/)（拓扑关联依赖统一的 service/dependency 语义）

**一手生产数据（作者博客）**

- [WebhookWise：我给告警群做了一个「守门人」](https://blog.wetalk.eu.org/ops/services/webhookwise-gatekeeper/)——在告警到达人之前加一层判断，本周"LLM 只在事件级触发、不在告警级触发"的设计在生产里怎么落。

**延伸阅读（公众号文章，须带批判性读）**

- 《每天上万条误报警，AI 如何让告警准确率提升 90%？》（公众号 篮球星探）——动态阈值 + 工作日/周末分基线的思路可以参考。但成果表的数字互相拼不出一致口径（「误报率 72%→9%」与同篇「15000 条里只有 400 条要处理」算不通，据此优化前应是 97%），标题的"90%"在正文里没有落点，示例数据的 5 天里含元旦和周末却被当成同质工作日基线——**正好反驳了它自己的核心论点**。当练习读。
- 《小团队零成本自建监控体系，告警从 200 条/天降到 5 条》（公众号 耕云躬行录）——架构判断和 PromQL 扎实，但**配置照抄会真出事**。建议作为本周的反向练习：不看答案，先找出文中这三处——(1) 一条 Alertmanager 抑制规则会在任意节点宕机时静默全集群的 Pod 告警；(2) 一段 Alloy 采集配置照抄即启动失败；(3) 一份 PrometheusRule 会被 Operator 一条都不加载。找齐了，本周"抑制规则是告警体系的灵魂"这句话就真懂了。答案见路线仓库的《AI 技术内容可信度判别指南》第 8 类。

### 实验三：写一个关联引擎（不带 LLM）

```
输入：一份真实或模拟的告警风暴（50+ 条，含症状告警和根因告警）
要求：
  1. 按 label 聚合成事件
  2. 用一张手工维护的服务依赖图，标出"疑似源头 vs 症状"
  3. 关联出时间窗内的变更事件
  4. 输出一个结构化 Incident（不调 LLM）
验证：压缩比多少？疑似源头对不对？如果把 LLM 接在后面，它的输入省了多少 token？
```

---

## 第 4 周：上下文工程（隐藏的成败变量）

### 要理解什么

工业界的共识（Meta、微软都印证）：**RCA 系统的天花板由"喂给模型的上下文质量"决定，不是模型参数量**。同一个 GPT/Claude，喂垃圾上下文得垃圾结论。上下文工程的核心是"在有限的 context window 里，塞进信噪比最高的证据"。

四个技巧：

```
① 检索而非灌入
   不要把全量日志/指标塞进去。按告警类型，检索"这类问题该看的那几样"。
   Meta 的做法：把候选变更压到 ≤20 个再让 LLM 排序，因为 context 装不下更多，
   且信噪比会随灌入量下降。

② 结构化 > 自然语言
   指标给"当前值/阈值/基线/趋势"四元组，别给一大段 PromQL 原始输出。
   变更给"服务/时间/作者/diff 摘要"，别给整个 commit。

③ 时间对齐
   所有证据带统一时间戳，让模型能重建时间线。
   "先 X 后 Y" 是因果推理的关键信号，乱序的证据会诱导错误结论。

④ 明确"缺什么"
   告诉模型"哪些数据源查询失败了/没数据"，
   否则它会把"没查到"当成"没问题"，这是 RCA 幻觉的一大来源。
```

### 一个高信噪比上下文的模板

```markdown
## 事件
时间：2026-08-24 14:30-14:45（持续中）
影响：api-gateway 5xx 从 0.1% 升至 32%，影响下单主链路
聚合告警：HighErrorRate(api-gateway) + PodNotReady(redis) [关联引擎判定为同一事件]

## 指标（当前值 / 阈值 / 24h基线 / 趋势）
api-gateway 5xx率：32% / 5% / 0.1% / ↑↑（14:30 起阶跃）
api-gateway P95：3.8s / 2s / 240ms / ↑↑
redis 可用副本：0 / - / 3 / 14:29 归零

## 近期变更（关联窗口 -30min）
14:28 redis-config 变更 by @someone：maxmemory 4gb→2gb  ← 时间最接近
（api-gateway 无变更）

## 数据源状态
✅ Prometheus / ✅ Loki / ⚠️ Jaeger 查询超时（trace 证据缺失）

## 任务
基于以上证据给出根因候选（含置信度）、证据链、下一步验证动作。
证据不足以定论时明确说明，并列出需要补充的数据。
```

对比一下：把这个模板喂给模型，和把"200 条原始告警 + 10MB 日志"喂给模型，成本和准确率是两个世界。

### 实验四：上下文 A/B

```
同一个事故，做两版上下文：
  A：原始数据堆砌（全量日志 + 原始告警）
  B：上面的结构化模板
喂给同一个模型，对比：token 数、根因质量、是否正确识别"trace 缺失"
```

---

## 第 5 周：带审批的自动修复（安全边界的硬机制）

### 要理解什么

这是整套系统风险最高的部分。一句话原则：**安全边界必须建立在系统机制上，不能建立在 prompt 或模型的"自觉"上**。"我在 system prompt 里写了不许删库"——这句话挡不住 prompt 注入，也挡不住模型幻觉。

### 分层防御（每一层都是硬机制）

```
第 1 层 权限（最硬）：
  Agent 用的 kubeconfig 就是只读 RBAC / 云 AK 就是只读策略
  → 它想删也删不掉。这一层是地基，其他层都是补充不是替代

第 2 层 动作白名单：
  可执行动作是一份显式枚举的列表，不是"生成任意命令"
  restart_pod / scale_deployment(±N) / rollback_to_previous —— 参数化、有 schema
  绝不允许"让 LLM 生成一段 shell 去执行"

第 3 层 风险分级 + 审批门：
  L0 只读：自动
  L1 低风险可逆（扩容无状态副本、重启单 Pod）：自动或轻审批
  L2 中风险（回滚、切流量）：必须人工确认
  L3 高风险（删数据、改权限、动存储）：禁止自动，只出建议

第 4 层 前置校验 + 后置验证：
  每个动作：前置条件检查 → 执行 → 成功判据 → 失败判据
  失败即停（circuit breaker），绝不"失败了再试一个别的"——那是扩大故障

第 5 层 审计 + 可回滚：
  每个动作存"决策快照"（当时的证据、置信度、谁批的）
  每个动作有明确回滚路径，且回滚本身也走这套流程
```

### 一个动作的完整定义（这是"工具"该有的样子）

```yaml
action: scale_deployment
risk_level: L1
params_schema:
  name: {type: string, required: true}
  namespace: {type: string, required: true}
  delta: {type: int, range: [-2, 5]}     # 硬约束：一次最多 +5，不能缩到 0 以下
preconditions:                            # 机制校验，不是 prompt 提醒
  - "deployment 存在且当前 ready"
  - "近 10min 无正在进行的 rollout"
  - "目标副本数 ≤ 该 namespace 配额"
approval:
  L1: auto_with_notify        # 自动执行但通知 owner
success_criteria:
  - "新副本 10min 内全部 ready"
  - "触发扩容的指标（如 P95）回落"
failure_action: stop_and_escalate   # 不满足即停 + 升级给人，绝不自动尝试其他动作
rollback: "scale 回原副本数"
audit: [triggering_incident, evidence_snapshot, decision_confidence, approver, result]
```

### 幂等与并发（容易被忽略的坑）

```
幂等：同一修复被触发两次（告警抖动/重复 webhook），不能扩容两次。
      每个修复动作绑定 incident_id，做去重。
并发：AI 在扩容的同时，HPA 也在扩容，人也在手动改 —— 三方打架。
      修复执行期间应"接管"该资源的自动化（暂停 HPA/加锁），完成后归还。
```

### 必读资料

- [Google SRE：Emergency Response / Managing Incidents](https://sre.google/sre-book/managing-incidents/)
- [OpenAI Agents SDK：Guardrails](https://openai.github.io/openai-agents-python/guardrails/)（输入输出护栏思想，但记住：guardrail 是补充，权限才是地基）

**一手生产数据（作者博客）**

- [只读是构造出来的，不是许诺出来的：让 AI Agent 碰生产的安全设计](https://blog.wetalk.eu.org/ops/services/read-only-by-construction/)——四层构造式只读（tool-call veto hook、只读 MCP 面、不可信输入围栏、双闸门修复），**每一层都假设上一层已经被骗过**。这是本周"安全边界必须是机制不是 prompt"的完整实现。
- [让 Agent 自己长经验，又不让它改自己的指令](https://blog.wetalk.eu.org/ops/services/agent-instruction-boundary/)——经验层与指令层的写权限分离，防止自我改进演变成自我越权。

**延伸阅读（公众号文章，须带批判性读）**

- 《别让 AI 直接碰生产：AI 落地运维的五阶段与三档授权》（公众号 叶哥知远）——按权限一格一格放开（SRE Assistant → Copilot → Workflow → Agent → 写权限）的分级框架站得住，可作团队定级的参照。**但两处会误导权限设计**：文中"MCP Server / MCP Tool 全部只读"——MCP 协议层没有任何只读的强制机制，规范里的 `readOnlyHint` 明确只是提示，只读只能由服务端实现和它持有的凭据保证；"低风险 → 自动执行所有查询类操作"忽略了读操作的爆炸半径（读到 secret、大查询打爆 querier、SaaS 按扫描量计费）。
- 反例练习：《我写了个服务器巡检 Skill，从一个小时压到 6 分钟》（公众号 码农庄园的运维师）——文中写"巡检全程只读，数据库仅执行 SELECT，安全放心"，请在同一篇里找出与之矛盾的那张表和上游工具的实际能力。这是《判别指南》第 9 类"把有写能力的工具描述成只读"的标准样本，也是本周责任模型要防的那种事。

### 实验五：设计并实现一个 L1 修复动作

```
选一个真正低风险的动作（推荐：重启单个无状态 Pod）
完整实现五层：
  1. 只读之外单独一个"能重启 Pod 但不能删 Deployment"的 RBAC
  2. 动作 schema + 参数校验
  3. 前置条件检查（Pod 属于无状态服务？近期没重启过？）
  4. 执行 + 成功/失败判据 + 失败即停
  5. 审计记录 + 幂等（同 incident 不重复重启）
验证：故意触发两次，看幂等生效；故意让前置条件不满足，看它拒绝执行
```

---

## 第 6 周：生产化、灰度与治理

### 灰度接管的正确姿势

```
不要"一夜之间让 AI 接管所有告警"。按维度逐步放开：
  按类型：先接管它在影子期证明最靠谱的那一类（如"磁盘将满"这种确定性强的）
  按环境：dev → staging → 非核心 prod → 核心 prod
  按动作：先只读诊断，再 L1 自动，最后才谈 L2 审批
  按时段：先工作时间（人能兜底）→ 再夜间
每一步都有回退开关（feature flag），异常一键切回人工
```

### 持续治理

```
Prompt/workflow 版本管理：像管代码一样，改动走评估集回归后才上线
成本治理：LLM 只在事件级触发（见第 3 周）；跟踪每次调用 token/成本；
          设人均/团队成本上限
模型漂移：模型升级 = 一次可能的回归，必须重跑评估集，别默认"新模型更好"
知识时效：Runbook 过期 → Agent 会给过期建议。要有 Runbook 复检机制
反馈闭环：人推翻 AI 结论时，记录"为什么"，喂回评估集 —— 这是系统变好的唯一正道
```

### AI SRE 产品格局（2026，知道有哪些轮子）

上生产前先想清楚"自建还是买"。2026 年这个品类已经成型：

```
纯自主 SRE Agent：Cleric / Resolve.ai / Traversal（独立产品，专注自主调查+RCA）
可观测平台内置：Datadog Bits AI SRE、（各大厂 Q2 2026 都跟进了 SRE Agent）
事件管理 + AI：Rootly / incident.io（AI 作为人工流程的加速器，不是自主 Agent）
```

自建的理由通常是：数据不能出域、要深度绑定自家 CMDB/依赖图、要完全掌控修复动作的安全边界。否则先评估买。

### 必读资料

- [Datadog：Bits AI SRE 发布](https://investors.datadoghq.com/news-releases/news-release-details/datadog-launches-bits-ai-sre-agent-resolve-incidents-faster)
- [Google SRE：Postmortem Culture](https://sre.google/sre-book/postmortem-culture/)（反馈闭环的文化基础）

**一手生产数据（作者博客）**

- [「已发送」标记撒了几周的谎：一次静默数据丢失的解剖](https://blog.wetalk.eu.org/ops/sent-marker-that-lied/)——AI 运维系统自己的静默失败复盘：状态标记与实际投递脱钩，几周后才发现。上生产前值得对照着问一遍：我的系统里哪些"成功"标记是自报的，哪些是被独立确认的。

**延伸阅读（公众号文章）**

- 《全网都在问：为什么很多企业做了 AIOps，最后却只得到一个"高级监控平台"？》（公众号 程序员喵手）——为什么落地会退化成"监控 2.0"的组织与治理视角，与本周"自建还是买"的判断互补。这篇难得地干净：通篇没有引用任何机构、报告或百分比，两处虚构场景都明确标了"假设"。唯一要注意的是它自己提出的五条度量指标里有三条分母不可测——写实验六的决策备忘录时，先把指标的可测性过一遍。

### 实验六：写一份《AI SRE 上生产决策备忘录》

```
一页纸，答清楚：
  自建还是买？理由（数据合规/定制深度/成本）
  第一个接管的告警类型是什么？为什么选它？
  灰度四维度（类型/环境/动作/时段）各定在哪一格？
  回退开关怎么设计？
  责任模型：出事谁负责、怎么审计
  成功指标：3 个月后用什么数字证明它有价值（MTTR? 降噪比? 人工推翻率?）
```

---

## 综合作业：端到端 AI SRE 系统设计答辩

把八个阶段的能力汇总成一个系统设计（纸面 + 关键部分原型）：

```
输入：告警风暴
  → [关联引擎] 压成 Incident（规则+拓扑，无 LLM）
  → [上下文工程] 收集高信噪比证据快照
  → [RCA Agent] 根因候选 + 证据链 + 置信度（L2：给人；证据不足：弃权升级）
  → [修复决策] 风险分级 → L1 自动带审计 / L2 人工审批 / L3 只建议
  → [执行] 前置校验 → 执行 → 后置验证 → 失败即停
  → [审计 + 反馈] 决策快照存档；人推翻则回流评估集

交付：
  架构图 + 数据流
  评估集（≥20 样本，含证据不足样本）+ 评估脚本
  一个真正实现的 L1 修复动作（五层防御齐全）
  灰度与责任模型备忘录
```

## 自测题

- Meta 的 RCA 系统准确率只有 42% 却被认为有价值，为什么？这对你定 KPI 有什么启示？
- RCA 系统的准确率上限主要由什么决定？（不是模型大小）依据是什么？
- 为什么 LLM 应该在"事件级"而非"告警级"触发？省了什么？
- 评估集里为什么必须有"证据不足"样本？它考的是 Agent 的什么能力？
- "在 prompt 里写不许删库"为什么不是安全边界？真正的地基是什么？
- 自动修复"失败后自动尝试另一个动作"为什么是危险设计？
- 修复动作的幂等性如果不做，会在什么场景出事？
- 影子模式为什么"只记录不展示"？
- 灰度接管的四个维度是什么？为什么不能一步到位？

## 完成标准

- 有一个能一键跑、含"证据不足"样本的评估流水线，并能演示"改坏 prompt → 指标下降"。
- 有一个不带 LLM 的告警关联引擎，能给出压缩比数据。
- 有一个五层防御齐全的 L1 修复动作实现，幂等和前置校验真实生效。
- 有一份 AI SRE 上生产决策备忘录，能答清责任模型和灰度策略。
- 能对着自己的系统设计讲清：每一处的安全边界是"机制"还是"prompt"，后者一律视为未完成。

这一阶段练出来的是"敢不敢上生产"的判断力：别人演示一个会查故障的 Agent，你能指出它缺评估、缺硬边界、缺审计，并说清补齐这三样各要做什么。这正是 AI SRE 从"炫技"到"可信"的全部距离。

## 配套可运行原型

本篇的评估流水线和五层防御修复动作，在 ai-ops-learning 仓库的 `ai-sre-prototype/` 有一份能跑的最小实现（后端本地 vLLM，另带零依赖的 mock 模式离线可跑）：

- `eval/run_eval.py`：20 样本评估集（含 5 个"证据不足"样本）+ 指标表，可实测"good prompt vs 删掉弃权规则的 broken prompt"——弃权召回从 100% 掉到 0%，验证评估集能抓 prompt 回归。
- `remediation/actions.py`：重启无状态 Pod 的 L1 动作，五层防御齐全，8 个单测覆盖每层 + 成功/失败路径。
- `demo_e2e.py`：RCA 判断 → 风险分级决策门 → L1 执行的完整串联（资源类触发重启、发布类只建议）。

先读该目录 README 跑通 mock 模式，再把 `--backend mock` 换成 `--backend vllm` 打你自己的 vLLM。
