# 第六阶段学习资料：云原生安全与身份

生成日期：2026-05-08  
对应路线文档：[工作路线完善版.md](</Users/imwl/Documents/New project/工作路线完善版.md>)

## 目标

第六阶段的目标是让平台不仅能跑，还能被治理、被审计、被约束。

阶段结束时，你应该能做到：

- 解释认证、授权、准入控制、运行时检测、审计之间的区别。
- 设计 Kubernetes RBAC 最小权限。
- 使用 Pod Security Admission、NetworkPolicy、ServiceAccount 做基础隔离。
- 使用 Kyverno 或 OPA Gatekeeper 写准入策略。
- 使用 Falco 做运行时检测，并理解规则调优成本。
- 理解 OAuth2、OIDC、SAML、Dex、Keycloak 的职责。
- 理解 SPIFFE / SPIRE workload identity。
- 设计一套“上线前阻断、运行中检测、事后审计”的安全闭环。

## 学习顺序

```text
Kubernetes 安全模型
    ↓
RBAC / ServiceAccount / Secret
    ↓
Pod Security / NetworkPolicy
    ↓
Admission Control
    ↓
Kyverno / OPA Gatekeeper
    ↓
镜像签名与准入
    ↓
Falco 运行时检测
    ↓
OIDC / OAuth2 / SAML / Dex / Keycloak
    ↓
SPIFFE / SPIRE workload identity
```

## 第 1 周：Kubernetes 安全模型

### 要理解什么

一次 Kubernetes API 请求通常经过：

```text
Authentication
    ↓
Authorization
    ↓
Admission Control
    ↓
Persistence
```

你要区分：

- 认证：你是谁。
- 授权：你能做什么。
- 准入：请求进入存储前是否允许或修改。
- 审计：发生了什么。
- 运行时检测：运行后是否出现异常行为。

### 必读资料

- [Kubernetes Authentication](https://kubernetes.io/docs/reference/access-authn-authz/authentication/)
- [Kubernetes Authorization](https://kubernetes.io/docs/reference/access-authn-authz/authorization/)
- [Kubernetes RBAC](https://kubernetes.io/docs/reference/access-authn-authz/rbac/)
- [Kubernetes Admission Controllers](https://kubernetes.io/docs/reference/access-authn-authz/admission-controllers/)

### 实验一：最小权限 RBAC

创建一个只能查看 Pod 的 ServiceAccount：

```bash
kubectl create namespace sec-demo
kubectl -n sec-demo create serviceaccount pod-reader

kubectl -n sec-demo create role pod-reader \
  --verb=get,list,watch \
  --resource=pods

kubectl -n sec-demo create rolebinding pod-reader \
  --role=pod-reader \
  --serviceaccount=sec-demo:pod-reader
```

验证：

```bash
kubectl auth can-i list pods --as=system:serviceaccount:sec-demo:pod-reader -n sec-demo
kubectl auth can-i delete pods --as=system:serviceaccount:sec-demo:pod-reader -n sec-demo
```

你要回答：

- Role 和 ClusterRole 的区别是什么？
- RoleBinding 能否绑定 ClusterRole？
- 为什么默认 ServiceAccount 不应该乱给权限？

## 第 2 周：Pod Security、Secret 与 NetworkPolicy

### 要理解什么

基础安全基线：

- 禁止 privileged。
- 禁止 hostPath 滥用。
- 禁止 hostNetwork 滥用。
- 非 root 用户运行。
- 只读 root filesystem。
- 限制 Linux capabilities。
- 设置 seccomp。
- namespace 级 Pod Security Admission。
- 默认拒绝网络策略。

### 必读资料

- [Pod Security Admission](https://kubernetes.io/docs/concepts/security/pod-security-admission/)
- [Pod Security Standards](https://kubernetes.io/docs/concepts/security/pod-security-standards/)
- [Network Policies](https://kubernetes.io/docs/concepts/services-networking/network-policies/)
- [Kubernetes Secrets](https://kubernetes.io/docs/concepts/configuration/secret/)

### 实验二：Pod Security Admission

```bash
kubectl create namespace restricted-demo
kubectl label namespace restricted-demo \
  pod-security.kubernetes.io/enforce=restricted \
  pod-security.kubernetes.io/audit=restricted \
  pod-security.kubernetes.io/warn=restricted
```

尝试创建 privileged Pod：

```bash
cat <<'EOF' | kubectl apply -n restricted-demo -f -
apiVersion: v1
kind: Pod
metadata:
  name: privileged
spec:
  containers:
  - name: app
    image: nginx
    securityContext:
      privileged: true
EOF
```

你要回答：

- enforce、audit、warn 的区别是什么？
- restricted baseline 会阻断哪些行为？
- 为什么 PodSecurityPolicy 被替代后，要使用 Pod Security Admission？

### 实验三：默认拒绝网络策略

注意：NetworkPolicy 是否生效取决于 CNI 是否支持。

```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
```

你要回答：

- NetworkPolicy 是 namespace 级还是集群级？
- 没有支持 NetworkPolicy 的 CNI 时会怎样？
- DNS egress 如何放行？

## 第 3 周：Admission Control、Kyverno 与 Gatekeeper

### 要理解什么

准入控制用于上线前阻断或修正风险。

适合准入控制的规则：

- 禁止 latest 镜像。
- 必须设置 resources。
- 必须有 owner 标签。
- 禁止 privileged。
- 限制镜像仓库来源。
- 禁止 hostPath。

不适合准入控制的事情：

- 复杂业务审批。
- 需要大量外部查询且可能超时的逻辑。
- 运行时行为判断。

### 必读资料

- [Kubernetes Policies](https://kubernetes.io/docs/concepts/policy/)
- [OPA for Kubernetes](https://www.openpolicyagent.org/docs/kubernetes)
- [Gatekeeper Documentation](https://open-policy-agent.github.io/gatekeeper/website/docs/)
- [Kyverno Documentation](https://kyverno.io/docs)
- [Kyverno Policies](https://kyverno.io/policies)

### 实验四：Kyverno 禁止 latest 镜像

安装 Kyverno 后，写策略：

```yaml
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: disallow-latest
spec:
  validationFailureAction: Enforce
  rules:
  - name: require-image-tag
    match:
      any:
      - resources:
          kinds:
          - Pod
    validate:
      message: "Using image tag latest is not allowed"
      pattern:
        spec:
          containers:
          - image: "!*:latest"
```

你要回答：

- audit 模式和 enforce 模式分别适合什么阶段？
- Kyverno mutate、validate、generate、verifyImages 分别解决什么？
- 准入策略出问题会不会影响集群可用性？

## 第 4 周：镜像签名与供应链

### 要理解什么

镜像安全不仅是扫描漏洞，还包括：

- 镜像来源可信。
- 镜像 tag 不可变。
- 镜像签名验证。
- SBOM。
- 构建 provenance。
- 部署时准入。

建议先掌握：

- digest 优先于 mutable tag。
- 只允许可信 registry。
- 关键环境需要签名验证。
- CI 产物和 CD 准入要打通。

### 必读资料

- [Kyverno Verify Images](https://kyverno.io/docs/policy-types/cluster-policy/verify-images/)
- [Sigstore Cosign](https://docs.sigstore.dev/cosign/)
- [SLSA Framework](https://slsa.dev/spec/v1.0/about)

### 实验五：镜像准入策略设计

写一份策略设计：

```text
允许的 registry：
是否允许 latest：
是否强制 digest：
是否强制签名：
签名身份：
CI 如何签名：
CD 如何验证：
失败时如何回滚：
例外流程：
```

你要回答：

- tag 和 digest 的安全差异是什么？
- 签名验证失败时是否允许人工绕过？
- 例外流程如何审计？

## 第 5 周：Falco 运行时检测

### 要理解什么

准入控制解决“上线前”，Falco 解决“运行中”。

Falco 可以检测：

- 容器里启动 shell。
- 写敏感路径。
- 访问敏感文件。
- 异常网络连接。
- 提权行为。
- Kubernetes API 异常操作。

重点是调优：

- 默认规则可能有噪声。
- 需要白名单预期行为。
- 告警要进入 SIEM / Slack / PagerDuty 等流程。
- 不能只安装不运营。

### 必读资料

- [Falco Documentation](https://falco.org/docs/)
- [Falco Getting Started](https://falco.org/docs/getting-started/)

### 实验六：检测容器内 shell

安装 Falco 后，进入某个 Pod：

```bash
kubectl exec -it <pod> -- sh
```

观察 Falco 事件。

你要回答：

- 这条告警是否一定是攻击？
- 如何为正常运维操作降噪？
- 运行时告警应该如何联动 incident？
- Falco 规则和 Kubernetes admission policy 的边界是什么？

## 第 6 周：OIDC、OAuth2、SAML、Dex、Keycloak

### 要理解什么

身份体系容易混：

- OAuth2 是授权框架。
- OIDC 是基于 OAuth2 的身份层。
- SAML 常用于企业 SSO。
- Dex 是 federated OIDC provider，常用于把 LDAP、GitHub、SAML 等桥接成 OIDC。
- Keycloak 是更完整的 IAM / IdP 产品。

### 必读资料

- [OpenID Connect Core 1.0](https://openid.net/specs/openid-connect-core-1_0.html)
- [OAuth 2.0 RFC 6749](https://www.rfc-editor.org/rfc/rfc6749)
- [SAML v2.0 OASIS Standard](https://www.oasis-open.org/standard/saml/)
- [Dex Documentation](https://dexidp.io/docs/)
- [Dex Kubernetes Guide](https://dexidp.io/docs/guides/kubernetes/)
- [Keycloak Documentation](https://www.keycloak.org/documentation)

### 实验七：设计 K8s SSO

写一份方案：

```text
身份源：
IdP：
OIDC issuer：
client_id：
groups claim：
RBAC 映射：
管理员组：
只读组：
审计方式：
离职回收：
break-glass 账号：
```

你要回答：

- OIDC 的 ID Token 和 Access Token 分别做什么？
- group claim 如何映射到 Kubernetes RBAC？
- break-glass 账号为什么需要？
- OIDC 失效时如何保证集群可恢复？

## 第 7 周：SPIFFE / SPIRE Workload Identity

### 要理解什么

人有身份，工作负载也需要身份。SPIFFE / SPIRE 解决 workload identity：

- SPIFFE ID 是工作负载身份。
- SVID 是可验证身份文档。
- SPIRE Server 负责注册和签发。
- SPIRE Agent 负责节点和工作负载 attestation。
- 可用于 mTLS、JWT、跨系统身份联邦。

### 必读资料

- [SPIRE Concepts](https://spiffe.io/docs/latest/spire-about/spire-concepts/)
- [SPIFFE Specifications](https://spiffe.io/docs/latest/spiffe-specs/)
- [Configuring SPIRE](https://spiffe.io/docs/latest/deploying/configuring/)
- [SPIRE Federation](https://spiffe.io/docs/latest/architecture/federation/readme/)

### 实验八：Workload Identity 设计

为服务设计身份：

```text
trust domain：
SPIFFE ID 格式：
workload selector：
证书轮转：
调用方授权：
跨集群信任：
与 Istio 身份关系：
```

你要回答：

- workload identity 和 Kubernetes ServiceAccount 是一回事吗？
- SPIFFE ID 如何用于 mTLS？
- 多集群如何建立信任？
- 静态密钥和动态身份的风险差异是什么？

## 命令清单

RBAC：

```bash
kubectl auth can-i <verb> <resource> --as=<user>
kubectl get role,rolebinding -A
kubectl get clusterrole,clusterrolebinding
kubectl describe rolebinding <name> -n <ns>
```

Pod 安全：

```bash
kubectl label namespace <ns> pod-security.kubernetes.io/enforce=restricted
kubectl get ns --show-labels
kubectl describe pod <pod>
```

NetworkPolicy：

```bash
kubectl get networkpolicy -A
kubectl describe networkpolicy <name> -n <ns>
```

Kyverno：

```bash
kubectl get clusterpolicy
kubectl get policyreport -A
kubectl describe clusterpolicy <name>
```

Falco：

```bash
kubectl get pods -n falco
kubectl logs -n falco -l app.kubernetes.io/name=falco
```

## 每周产出

- 第 1 周：Kubernetes 安全请求链路图。
- 第 2 周：Pod 安全基线与 NetworkPolicy 示例。
- 第 3 周：Kyverno / Gatekeeper 策略包。
- 第 4 周：镜像准入策略设计。
- 第 5 周：Falco 运行时检测实验。
- 第 6 周：Kubernetes SSO 方案。
- 第 7 周：Workload Identity 设计。

## 自测题

- Authentication、Authorization、Admission、Audit 的区别是什么？
- Role 和 ClusterRole 有什么区别？
- Pod Security restricted 会限制什么？
- NetworkPolicy 默认行为是什么？
- Kyverno 和 Gatekeeper 的差异是什么？
- 准入策略为什么要先 audit 后 enforce？
- Falco 告警如何降噪？
- OIDC 和 OAuth2 的关系是什么？
- SAML 和 OIDC 分别常用于什么场景？
- SPIFFE ID 和 ServiceAccount 的关系是什么？

## 完成标准

你可以认为第六阶段过关的标准是：

- 能写最小权限 RBAC。
- 能设计 Pod Security、NetworkPolicy、准入策略、运行时检测的组合基线。
- 能设计 K8s OIDC 登录和 RBAC 映射。
- 能解释 workload identity，并知道如何与 Service Mesh / mTLS 结合。
