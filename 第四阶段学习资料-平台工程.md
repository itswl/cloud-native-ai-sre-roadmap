# 第四阶段学习资料：平台工程

生成日期：2026-05-08  
对应路线文档：[工作路线完善版.md](工作路线完善版.md)

## 目标

第四阶段的目标不是“搭一个运维后台”，而是把运维能力产品化，给研发提供稳定、可复用、可审计的交付路径。

阶段结束时，你应该能做到：

- 解释 Internal Developer Platform、Internal Developer Portal、PaaS、GitOps、IaC 的区别。
- 用 Argo CD 或 Flux 管理多环境应用。
- 用 Kustomize / Helm 组织环境差异。
- 设计 GitOps repo 结构、同步策略、回滚策略和权限边界。
- 用 Terraform / OpenTofu 抽象基础设施模块，并理解 state、backend、lock、drift。
- 用 Backstage 或轻量门户做服务目录、模板、文档、K8s 可见性。
- 设计一条 golden path：从创建服务到发布、监控、告警、Runbook 全自动接入。

## 平台工程原则

平台工程的核心不是“平台团队控制一切”，而是给业务团队铺好路：

- 自助。
- 标准化。
- 可审计。
- 可回滚。
- 可观测。
- 默认安全。
- 降低认知负担。

你要避免两个极端：

- 平台太薄：只是一堆脚本和文档，研发还是到处找人。
- 平台太厚：什么都封死，研发无法灵活交付。

## 建议环境

- kind / k3d / 云上测试集群。
- Argo CD 或 Flux，二选一即可，建议先 Argo CD。
- Helm。
- Kustomize。
- Terraform 或 OpenTofu。
- 一个 Git 仓库，用于 GitOps 配置。
- 一个示例应用仓库。
- Backstage 可选；如果太重，先用 Markdown + 模板脚本模拟门户能力。

## 学习顺序

```text
平台工程与平台产品思维
    ↓
GitOps 原则
    ↓
Argo CD / Flux
    ↓
Kustomize / Helm 环境管理
    ↓
Progressive Delivery
    ↓
IaC：Terraform / OpenTofu
    ↓
Backstage / IDP / Golden Path
    ↓
平台治理与度量
```

## 第 1 周：平台工程与 Golden Path

### 要理解什么

平台不是工具集合，而是一组被产品化的能力。你需要定义平台客户、平台边界和服务等级。

平台能力通常包括：

- 服务脚手架。
- CI / CD。
- 部署模板。
- 配置与密钥管理。
- 监控与告警。
- 日志与 tracing。
- 权限与身份。
- 发布与回滚。
- 成本与资源规范。
- Runbook 与文档。

### 必读资料

- [CNCF Platforms White Paper](https://tag-app-delivery.cncf.io/whitepapers/platforms/)
- [CNCF：Internal developer platform vs portal vs PaaS](https://www.cncf.io/blog/2023/12/08/internal-developer-platform-vs-internal-developer-portal-vs-paas/)

### 实验一：定义你的平台服务目录

写一份平台能力清单：

```text
目标用户：
平台不负责什么：
服务创建能力：
发布能力：
观测能力：
安全能力：
成本能力：
故障处理能力：
SLA / SLO：
支持渠道：
```

你要回答：

- 研发创建一个新服务时，最痛的 5 个步骤是什么？
- 哪些步骤可以模板化？
- 哪些步骤必须人工审批？
- 平台的 owner 和业务服务 owner 如何分工？

## 第 2 周：GitOps 原则

### 要理解什么

GitOps 不是“把 YAML 放 Git 里”这么简单。核心原则是：

- 声明式。
- 期望状态版本化。
- 自动拉取。
- 持续 reconcile。

你要理解：

- Git 是期望状态来源。
- 集群内 controller 拉取并对比状态。
- drift 应该被发现和修复。
- 直接改集群会造成 Git 与实际状态不一致。
- Secret、镜像 tag、环境差异需要专门设计。

### 必读资料

- [OpenGitOps](https://opengitops.dev/)
- [Argo CD Documentation](https://argo-cd.readthedocs.io/en/stable/)
- [Flux Documentation](https://fluxcd.io/flux/)

### 实验二：设计 GitOps 目录结构

示例：

```text
gitops-repo/
  clusters/
    dev/
      apps/
      platform/
    staging/
      apps/
      platform/
    prod/
      apps/
      platform/
  apps/
    demo/
      base/
      overlays/
        dev/
        staging/
        prod/
```

你要回答：

- app repo 和 gitops repo 要不要分开？
- 镜像 tag 由谁更新？
- prod 环境如何审批？
- 如果有人 `kubectl edit` 了线上资源，平台如何发现 drift？

## 第 3 周：Argo CD 实战

### 要理解什么

Argo CD 是 GitOps controller。你要掌握：

- Application。
- App of Apps。
- Project。
- Sync policy。
- Prune。
- Self-heal。
- Sync waves。
- Health check。
- Diff。
- RBAC。

### 必读资料

- [Argo CD Getting Started](https://argo-cd.readthedocs.io/en/stable/getting_started/)
- [Argo CD Application Specification](https://argo-cd.readthedocs.io/en/stable/user-guide/application-specification/)
- [Argo CD Sync Waves](https://argo-cd.readthedocs.io/en/stable/user-guide/sync-waves/)

### 实验三：部署一个多环境应用

安装 Argo CD：

```bash
kubectl create namespace argocd
kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
kubectl -n argocd get pods
```

创建一个 Application，指向你的 GitOps repo。

你要实现：

- dev 自动同步。
- staging 手动同步。
- prod 需要 PR 合并后同步。
- 开启 prune 和 selfHeal 前先解释风险。

你要回答：

- prune 为什么危险？
- selfHeal 什么时候有价值？
- Argo CD 的健康状态和 Kubernetes ready 状态是否一样？
- Sync wave 能解决哪些依赖顺序问题，不能解决什么？

## 第 4 周：Kustomize、Helm 与环境差异

### 要理解什么

环境差异一定会存在，但不能失控。

常见差异：

- 副本数。
- resource requests / limits。
- 域名。
- HPA。
- feature flag。
- Secret 引用。
- 数据库地址。
- 告警阈值。

Kustomize 适合 overlay patch；Helm 适合 chart 参数化。二者可以结合，但要控制复杂度。

### 必读资料

- [Kustomize 官方文档](https://kubectl.docs.kubernetes.io/references/kustomize/)
- [Helm Documentation](https://helm.sh/docs/)
- [Argo CD Helm](https://argo-cd.readthedocs.io/en/stable/user-guide/helm/)
- [Argo CD Kustomize](https://argo-cd.readthedocs.io/en/stable/user-guide/kustomize/)

### 实验四：环境 overlay

目标：

```text
base:
  Deployment
  Service
  PrometheusRule
  ServiceMonitor

dev:
  replicas=1
  low resources

prod:
  replicas=3
  PDB
  HPA
  stricter alert
```

你要回答：

- 哪些差异适合 Kustomize patch？
- 哪些差异适合 Helm values？
- 环境差异太多说明什么问题？

## 第 5 周：Progressive Delivery

### 要理解什么

平台发布能力不能只支持全量发布。你要理解：

- rolling update。
- blue-green。
- canary。
- traffic splitting。
- automated analysis。
- manual promotion。
- rollback。

Argo Rollouts 可以与 Ingress、Service Mesh、Gateway、metrics provider 结合做渐进式发布。

### 必读资料

- [Argo Rollouts Documentation](https://argo-rollouts.readthedocs.io/en/stable/)
- [Argo Rollouts Canary](https://argo-rollouts.readthedocs.io/en/stable/features/canary/)
- [Argo Rollouts BlueGreen](https://argo-rollouts.readthedocs.io/en/stable/features/bluegreen/)

### 实验五：做 canary 发布

目标：

- 初始版本 v1。
- 发布 v2 先导 10%。
- 观察 5xx 和 latency。
- 手动 promote。
- 失败时 rollback。

你要回答：

- canary 的成功条件是什么？
- 分流是在 Service、Ingress、Gateway 还是 Mesh 层完成？
- 自动回滚的指标应该从哪里来？
- 为什么发布系统必须接入可观测性？

## 第 6 周：IaC：Terraform / OpenTofu

### 要理解什么

IaC 的重点不是写 resource，而是管理基础设施生命周期。

重点概念：

- provider。
- resource。
- module。
- state。
- remote backend。
- state lock。
- plan / apply。
- drift。
- sensitive output。
- import。

### 必读资料

- [Terraform State](https://developer.hashicorp.com/terraform/language/state)
- [Terraform Modules](https://developer.hashicorp.com/terraform/language/modules)
- [OpenTofu State](https://opentofu.org/docs/language/state/)
- [OpenTofu Modules](https://opentofu.org/docs/language/modules/)

### 实验六：抽象基础设施模块

设计一个模块：

```text
service-infra-module
  DNS
  certificate
  database
  redis
  object storage
  monitoring labels
  tags
```

你要回答：

- state 文件为什么敏感？
- remote backend 为什么需要锁？
- drift 怎么发现？
- 什么时候应该 import 现有资源？
- module 的输入输出如何避免泄漏密钥？

## 第 7 周：Backstage 与内部开发者门户

### 要理解什么

Backstage 的价值不是页面，而是服务目录和标准化入口。

重点能力：

- Software Catalog。
- Software Templates。
- TechDocs。
- Kubernetes plugin。
- ownership。
- service metadata。
- scorecards 可选。

### 必读资料

- [Backstage：What is Backstage?](https://backstage.io/docs/overview/what-is-backstage/)
- [Backstage Software Templates](https://backstage.io/docs/features/software-templates/)
- [Backstage TechDocs](https://backstage.io/docs/features/techdocs/getting-started/)
- [Backstage Kubernetes Plugin](https://backstage.io/docs/features/kubernetes/)

### 实验七：设计服务模板

模板生成：

```text
repo
Dockerfile
CI pipeline
Helm chart / Kustomize
ServiceMonitor
PrometheusRule
Grafana dashboard link
Runbook
catalog-info.yaml
```

你要回答：

- 模板要暴露哪些参数？
- 哪些默认值不能让研发乱改？
- 服务 owner 如何维护？
- Runbook 如何跟服务目录关联？

## 第 8 周：平台治理与度量

### 要理解什么

平台本身也要被度量。

建议指标：

- 新服务创建耗时。
- 部署频率。
- 变更失败率。
- 平均恢复时间。
- 平台模板采用率。
- 手工工单数量。
- 告警接入率。
- Runbook 覆盖率。
- 服务 owner 完整率。

### 实验八：平台成熟度评分

为每个服务打分：

```text
是否有 owner：
是否有 SLO：
是否有 dashboard：
是否有告警：
是否有 Runbook：
是否接入 tracing：
是否 GitOps 管理：
是否有 PDB：
是否有 HPA：
是否符合安全基线：
```

你要回答：

- 平台要推动标准，还是强行阻断？
- 哪些规则适合 admission policy？
- 哪些规则适合平台评分？
- 如何避免平台团队变成工单团队？

## 命令清单

Argo CD：

```bash
kubectl -n argocd get pods
kubectl get applications -A
kubectl describe application <name> -n argocd
argocd app list
argocd app get <name>
argocd app sync <name>
argocd app diff <name>
argocd app rollback <name>
```

Kustomize / Helm：

```bash
kustomize build overlays/dev
kustomize build overlays/prod
helm template demo ./chart -f values-dev.yaml
helm lint ./chart
```

Terraform / OpenTofu：

```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform apply
terraform state list
terraform import
tofu init
tofu plan
```

## 每周产出

- 第 1 周：平台能力清单。
- 第 2 周：GitOps repo 设计。
- 第 3 周：Argo CD 多环境应用。
- 第 4 周：Kustomize / Helm 环境模板。
- 第 5 周：canary 发布实验。
- 第 6 周：IaC 模块设计。
- 第 7 周：服务模板设计。
- 第 8 周：平台成熟度评分。

## 自测题

- GitOps 和 CI/CD 的区别是什么？
- 为什么 GitOps controller 通常在集群内拉取状态？
- prune 和 selfHeal 有什么风险？
- Kustomize 和 Helm 分别适合什么场景？
- canary 自动回滚依赖哪些指标？
- Terraform state 为什么要保护？
- drift 是怎么产生的？
- Backstage 的 Catalog 和 Template 分别解决什么问题？
- 平台工程和传统运维后台最大的区别是什么？

## 完成标准

你可以认为第四阶段过关的标准是：

- 能让一个新服务通过模板创建并自动接入发布、监控、告警、文档。
- 能用 GitOps 管理多环境部署，并解释同步、差异、回滚和权限边界。
- 能写出一个 IaC 模块并说明 state 与 drift 风险。
- 能用平台评分推动服务标准化，而不是靠人肉催。
